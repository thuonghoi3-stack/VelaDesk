ccxt>=4.4.0
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=16.0.0
plotly>=5.24.0
pyyaml>=6.0.2
python-dotenv>=1.0.1
pytest>=8.3.0

# Indicators are computed in src/features/indicators.py (pure pandas) — no
# pandas-ta needed. The event-driven engine in src/backtest/engine.py plays
# the backtrader role; add heavy backtest frameworks only if you really
# cross-check results against them.
